import React from 'react';
import TopNav from './components/TopNav/TopNav';

const App = () => {
    return (
        <div id='app'>
            Home
        </div>
    );
};

export default App;

// Make a section for 'Sample Searches' as suggestions to lookup data from the API.

// Book Cover Pages
// Import list of books from Google Books API
// Make whole page (for one book viewing) UI look like the book cover of "Is This Anything?" by Jerry Seinfeld

// Maybe add a Drawer from v4mui to the left side of the page, w/options for 'Data', 'Case Studies', 'Testing', 'Articles'

// Use a Stepper for the home page (App) to show the user the steps to view all of the data.

// One man’s crappy software is another man’s full time job. (Jessica Gaston)